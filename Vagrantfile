# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "coreos-alpha"
  config.vm.network :private_network, ip: '192.168.88.88'
  config.vm.hostname = 'coreos-1'
end
